# 原版 Tsugu Bot 功能对照

## 对照基线

- 原版仓库：`Yamamoto-2/tsugu-bangdream-bot`
- 对照提交：`0b0607f31e8d93fc80646ad05537cbe43ce50913`
- 对照范围：`src/index.ts` 中当前实际注册的命令、`src/commands/` 行为、用户数据、车牌流程及后端路由契约
- AstrBot 版本：`2.1.0`

## 命令矩阵

| 原版命令 | 原版别名/快捷命令 | AstrBot 实现 | 状态 |
|---|---|---|---|
| `开启车牌转发` | 无 | `开启车牌转发` | 完整 |
| `关闭车牌转发` | 无 | `关闭车牌转发` | 完整 |
| `绑定玩家` | 无 | `玩家绑定`、`绑定玩家` | 完整 |
| `解除绑定` | `解绑玩家` | `解除绑定`、`解绑玩家` | 完整 |
| `主服务器` | `服务器模式`、`切换服务器`、`日服模式`等 | 同名、别名及五服务器快捷命令 | 完整 |
| `设置显示服务器` | `默认服务器`、`设置默认服务器` | `显示服务器`及全部原版名称 | 完整 |
| `玩家状态` | `日服玩家状态`等 | 同名及五服务器快捷命令 | 完整 |
| `玩家状态列表` | `玩家列表`、`玩家信息列表` | `绑定列表`及全部原版名称 | 完整 |
| `玩家默认ID` | `默认玩家ID`、`默认玩家`、`玩家ID` | `选择绑定`及全部原版名称 | 完整 |
| `ycm` | `有车吗`、`车来` | `车牌列表`及全部原版名称 | 完整 |
| `查玩家` | `查询玩家` | 同名及别名 | 完整 |
| `查卡` | `查卡牌` | 同名及别名 | 完整 |
| `查卡面` | `查卡插画`、`查插画` | 同名及别名 | 完整 |
| `查角色` | 无 | `查角色` | 完整 |
| `查活动` | 无 | `查活动` | 完整 |
| `查曲` | 无 | `查曲` | 完整 |
| `查谱面` | 无 | `查谱面` | 完整 |
| `随机曲` | `随机` | `随机曲目`、`随机曲`、`随机` | 完整 |
| `查询分数表` | `查分数表`、`查询分数榜`、`查分数榜` | `分数表`及全部原版名称 | 完整 |
| `查试炼` | `查stage`、`查舞台`、`查festival`、`查5v5` | 同名及全部别名 | 完整 |
| `查卡池` | 无 | `查卡池` | 完整 |
| `ycx` | 无 | `ycx` | 完整 |
| `ycxall` | `myycx` | 同名及别名 | 完整 |
| `lsycx` | 无 | `lsycx` | 完整 |
| `抽卡模拟` | 无 | `抽卡模拟`，另提供抽卡/单抽/十连便利别名 | 完整 |

## 行为对齐

- 用户默认值与原版一致：主服务器为国服，显示顺序为国服、日服，车牌转发默认开启，默认玩家索引为 0。
- 服务器参数支持原版的数字、缩写、中文全称、中文简称和后端模糊搜索。
- 谱面难度支持原版模糊搜索，并补充简单、普通、困难、专家、特殊中文名称。
- `抽卡模拟` 使用原版位置参数 `[次数] [卡池ID]`，默认 10 抽，并支持原版文档中的 `300 922`。
- 绑定与解绑保留远程验证码流程；验证后续消息及取消消息不受命令唤醒开关阻断。
- 车牌识别、真假关键词、毫秒时间戳、平台映射、用户头像、转发开关和静默提交行为与原版一致。
- 国服档线列表包含原版的 1500 档。

## 框架等价实现

原版可在 Koishi 本地数据库和远程用户数据库之间切换。AstrBot 插件使用 Tsugu 用户数据后端保存相同的用户字段和绑定状态，这是跨框架后的等价持久化实现，不需要复制 Koishi 表结构。

原版配置中的回复引用和 @发送人分别对应 AstrBot 的 `quote_reply_enabled` 与 `at_sender_enabled`。原版命令权限由 Koishi authority 提供；AstrBot 版本使用 AstrBot 自身权限体系，并额外提供群白名单与 @唤醒控制。

## 不属于当前启用功能

原版源码中的“群聊抽卡开关”和 `tsugu_swc` 位于注释块内，没有在当前版本注册为可用命令，因此不计入当前功能移植范围。若上游未来重新启用，应在升级时重新纳入矩阵。

## 验证范围

- 参数解析、用户数据容错、别名注入、绑定会话、快捷命令、空车列表和车牌提交均有自动化回归测试。
- 车牌请求通过真实 `tsugu_api_core` 网络层连接本地 HTTP 服务，校验完整请求体和毫秒时间戳。
- 发布前使用 AstrBot 自带 Python 环境执行原生导入、处理器注册和元数据解析检查。
